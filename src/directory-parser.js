const fs = require('fs')
const path = require('path')

function parsePagesDirectory(
  dir,
  { prependName, prependPath } = { prependName: '', prependPath: '/' },
) {
  let routes = []

  //TODO

  return { routes }
}

module.exports = {
  parsePagesDirectory,
}
